/**
 * Esquema Drizzle — fonte da verdade do banco.
 *
 * Vazio de propósito: a DDL canônica (enums, `orgs`, livros-caixa, `partners`,
 * `bookings`, triggers e RLS) entra na **Etapa 3**, tabela por tabela, com as
 * constraints que sustentam as invariantes 3, 6 e 7.
 *
 * Toda mudança daqui vira arquivo versionado em `supabase/migrations/` via
 * `npm run db:generate`. Nunca alterar esquema pelo painel do Supabase.
 */

export {};
